# Loaded by `vite dev` (`make run`). Committed — public, non-secret values only.
# Vite inlines `VITE_`-prefixed vars into the client bundle.
# Override locally in `.env.development.local` (gitignored).
VITE_API_URL=http://localhost:8000
